ARG BUILD_FROM
FROM $BUILD_FROM

ENV NODE_ENV=production
ENV PORT=3000

# The base image already has the correct ENTRYPOINT and CMD for the slicer API.
# We are simply wrapping it as a Home Assistant Add-on.
