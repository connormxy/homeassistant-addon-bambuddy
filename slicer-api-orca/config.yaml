name: "OrcaSlicer API (Bambuddy)"
version: "2.4.2.3"
slug: bambuddy_orcaslicer_api
description: "A headless OrcaSlicer sidecar container for cloud-independent slicing in Bambuddy."
url: "https://github.com/connormxy/homeassistant-addon-bambuddy"
image: "ghcr.io/connormxy/{arch}-slicer-api-orca"
arch:
  - aarch64
  - amd64
startup: services
boot: auto
init: false
map:
  - share:rw
ports:
  3003/tcp: 3003
ports_description:
  3003/tcp: OrcaSlicer API Port (use this in Bambuddy settings)

healthcheck:
  enabled: true
  interval: 30s
  timeout: 5s
  retries: 3

image: "ghcr.io/connormxy/slicer-api-orca-{arch}"
